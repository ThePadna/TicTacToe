import React, { Component } from 'react';

class Game {

    constuctor(size, ai) {
        
    }

    draw(size) {
        let ctx = document.getElementById("gamecanvas").getContext("2d");
        //..
    }
}

export default Game;